[package]
name = "hadamard-construct"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true

[dependencies]
hadamard-core = { path = "../core" }

[lib]
name = "hadamard_construct"
path = "src/lib.rs"
