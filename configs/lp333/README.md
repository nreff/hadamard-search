# LP(333) Search Configs

These config files are reproducible starting points for the `hadamard search lp --config ...` flow.

- `compressed3-smoke.cfg`: bounded smoke-test slice for validating the `333 / 3` compressed path
- `compressed3-shard0of16.cfg`: first shard of a broader compressed run plan

CLI flags still override config-file values when needed.
