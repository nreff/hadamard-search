# TODO

Primary dashboard:

- see [`docs/PROJECT_STATE.md`](docs/PROJECT_STATE.md) for current state and priority order

Execution rule:

- Do not mark any item complete until its milestone gates in `docs/MILESTONES.md` pass.

- Add a genuinely faster PSD/DFT backend behind the current pluggable interface.
- Add richer artifact schemas for compressed candidates and verified exact pairs.
- Add more known Legendre-pair and Hadamard fixtures beyond length 5 / order 12.
- Implement a real compressed decompressor interface.
- Add meet-in-the-middle SDS matching primitives.
- Add benchmark baselines for length 63 and length 111 preprocessing.
- Add more `uv`-managed Python helpers for artifact inspection and cross-checking.
